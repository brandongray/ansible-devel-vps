- name: install core packages
  yum:
    name: "{{ item }}"
    state: latest
  with_items:
    - epel-release
    - vim-enhanced
    - git
    - ansible
    - cronie

- name: start/enable crond
  service:
    name: crond
    state: started
    enabled: yes
